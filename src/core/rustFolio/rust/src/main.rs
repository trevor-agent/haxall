// rust-folio: Rust-backed Haxall Folio storage engine
// Milestone 0: stub entry point — crate compiles cleanly, exits cleanly.

fn main() {
    eprintln!("rust-folio: not yet implemented (M0 stub)");
    std::process::exit(0);
}
